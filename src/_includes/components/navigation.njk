<nav class="flex items-center justify-between flex-wrap py-3">
  <a href="/" class="font-semibold text-xl tracking-loose">Alex Logsdon</a>

  {% set navPages = collections.all | eleventyNavigation %}
  <ul>
    {% for entry in navPages %}
      <li class="inline">
        <a
         href="{{ entry.url }}"
         class="text-sm rounded ml-4 inline-block
         {{'text-orange' if entry.url in page.url
         else 'text-gray-700 hover:text-gray-900'}}">
         {{ entry.title }}
       </a>
      </li>
    {% endfor %}
 </ul>
</nav>
